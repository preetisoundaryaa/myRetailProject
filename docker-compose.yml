version: "3.9"

services:
  web:
    build: .
    container_name: retail-shelf-app
    ports:
      - "8000:8000"
    environment:
      APP_ENV: dev
      DEBUG: "true"
      LOG_LEVEL: DEBUG
    volumes:
      - ./:/app
    command: python -m app.main
