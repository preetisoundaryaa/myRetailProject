apiVersion: v1
kind: Service
metadata:
  name: retail-service
spec:
  type: NodePort
  selector:
    app: retail-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007
